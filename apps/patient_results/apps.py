from django.apps import AppConfig


class PatientResultsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.patient_results"
    verbose_name = "Patient results (portal wyniki)"
